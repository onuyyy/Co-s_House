spring:
  docker:
    compose:
      enabled: true
      file: ${COMPOSE_FILE:docker-compose.yml}
      lifecycle-management: start-and-stop

  datasource:
    # 로컬에서 Docker 컨테이너 MySQL에 접속 (root 계정 사용)
    url: jdbc:mysql://localhost:13306/cos?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  thymeleaf:
    enabled: true
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8
    mode: HTML

  mail:
    host: smtp.naver.com #smtp 서버 주소
    port: 465 # 메일 인증서버 포트
    username: yju407@naver.com #네이버 아이디
    password: JYVFH6QBY141 #네이버 비밀번호
    properties:
      mail:
        smtp:
          starttls:
            enable: false
          auth: true
          ssl:
            enable: true #반드시 필요 -> SSL 필요로 되어있으면
app:
  email:
    from: yju407@naver.com

server:
  port: 8080
