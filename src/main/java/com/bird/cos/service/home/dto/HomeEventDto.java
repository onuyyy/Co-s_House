package com.bird.cos.service.home.dto;

public record HomeEventDto(
        String eyebrow,
        String title,
        String description,
        String period,
        String linkUrl,
        String imageUrl
) {}
